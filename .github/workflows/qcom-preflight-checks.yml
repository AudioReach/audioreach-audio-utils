name: Qualcomm Preflight Checks

on:
  pull_request_target:
    branches: [ njjetha-patch-1 ]
  push:
    branches: [ njjetha-patch-1 ]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  qcom-preflight-checks:
    uses: Audioreach/audioreach-workflows/.github/workflows/qcom-preflight-checks.yml@checker
    secrets: inherit
